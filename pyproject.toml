[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "de-lu-epf"
version = "0.1.0"
description = "Energy forecasting project"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["legacy*"]